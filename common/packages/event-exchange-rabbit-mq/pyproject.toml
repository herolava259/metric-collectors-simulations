[project]
name = "event-exchange-rabbit-mq"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tung leduc", email = "chopperman259@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "pika>=1.3.2",
    "rstream>=0.40.0",
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
